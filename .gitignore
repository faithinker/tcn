# deps / build
node_modules/
dist/
.astro/
.wrangler/
coverage/

# verify harness output (screenshots)
verify/

# os / editor
.DS_Store
*.log
npm-debug.log*

# env / local secrets
.env
.env.*
!.env.example
.dev.vars
.dev.vars.*
translation-input.json
translation-output.json

# local scratch (plans, mockups) — 커밋 제외
.scratch/
wrangler.staging.jsonc

# claude runtime locks (프로세스 락 — 추적 대상 아님)
.claude/*.lock
.claude/settings.local.json
.gstack/

# 테스트 커버리지 리포트 (자동 생성물)
coverage/

# 브라우저 자동화 산출물
.playwright-mcp/

# Claude Code 병렬 작업 워크트리 (저장소 사본 — 추적·스캔 제외)
.claude/worktrees/

# Impeccable UI 크리틱 산출물 (어드민 취약점 인벤토리 — 공개 저장소 제외, 로컬 보관)
.impeccable/critique/
